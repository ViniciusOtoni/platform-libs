name: Release

on:
  push:
    branches: [main]

jobs:
  release:
    uses: ViniciusOtoni/mlops-platform/.github/workflows/release-package.yml@main
    with:
      working-directory: .
    secrets: inherit
